# Paste into a new AI coding session

The attached ZIP is the complete Minsah Beauty repository. Extract it and use `minsahbeauty-meta-v6-update/` as the repository root.

Before editing, run:

```bash
npm run ai:fast-start
```

This must confirm full repository access and print the current work packet. Current verified checkpoint is Phase 31 Layer 6; exact current item is **9.8 — Final runtime and release gate**. Do not restart Layers 1-6. Do not create per-item ZIPs. Never claim unexecuted build, database, Redis/WebSocket or live Meta evidence.
